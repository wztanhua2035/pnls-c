# 更新记录

## v1.1.0 高速叙事版

- 普通回合改为100至200字，重大剧情改为200至500字。
- `/api/game/turn` 改为NDJSON流式接口；支持OpenAI兼容SSE，若模型不支持流式则自动回退到完整响应。
- 页面先逐字显示剧情，完成后再显示四个选项并更新人物、道具和好感状态。
- 每回合只发送最近5回合原文、长期摘要、当前人物/道具/任务状态。
- 地图改为带关键词的分区资料，每回合最多选取两个相关地区，远离当前剧情的地点不发送。
- 未登场女主只向AI提供姓名与最早登场回合，不提供背景或好感度；页面也不显示其关系信息。
- 旧存档自动迁移长期摘要和已登场女主字段。
- 修复Zeabur `/data` 数据卷可能出现的写入权限问题。
- 后台遇到非JSON错误时显示明确HTTP状态，而不是 `Unexpected token`。

## 部署更新

将本文件夹的全部文件覆盖到Git仓库原项目目录并提交，等待Zeabur自动重新部署。不要删除或改动Zeabur挂载的 `/data` 数据卷，也不要更换 `APP_SECRET`。
